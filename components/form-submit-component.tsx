"use client";

import React from "react";
import { FormElementInstance } from "./form-elements";

export default function FormSubmitComponent({
  content,
  formUrl,
}: {
  content: FormElementInstance[];
  formUrl: string;
}) {
  return <div>FormSubmitCompoent</div>;
}
